FROM node:20-alpine

WORKDIR /app

COPY package.json ./
# Lockfile optional at phase 1; will be generated by first `npm install`.
RUN npm install

COPY tsconfig.json vite.config.ts index.html ./
COPY src ./src

EXPOSE 5173

CMD ["npm", "run", "dev"]
